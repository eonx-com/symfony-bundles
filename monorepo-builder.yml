parameters:
    package_directories:
        - 'bundles'

    # for "merge" command
    data_to_append:
        require-dev:
            symplify/monorepo-builder: '^6.1'

    # for "split" command
    directories_to_repositories:
        bundles/CoreBundle: 'git@github.com:loyaltycorp/core-bundle.git'
        bundles/ScheduleBundle: 'git@github.com:loyaltycorp/schedule-bundle.git'
